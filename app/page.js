export default function Home() {
  return (
    <div>
      {/* <h1>Data from SQLite:</h1>
      <ul>
        {data.map((item) => (
          <li key={item.id}>{JSON.stringify(item)}</li>
        ))}
      </ul> */}
    </div>
  );
}